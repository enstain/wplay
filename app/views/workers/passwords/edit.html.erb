<h2>Смените ваш пароль</h2>

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <%= f.input :reset_password_token, as: :hidden %>
  <%= f.full_error :reset_password_token %>

  <div class="form-inputs">
    <%= f.input :password, label: "Новый пароль", required: true, autofocus: true %>
    <%= f.input :password_confirmation, label: "Введите ещё раз", required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Сменить пароль" %>
  </div>
<% end %>

<%= render "worker/shared/links" %>
