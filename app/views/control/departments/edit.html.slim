= render "partials/flash"

= simple_form_for @department, url: control_department_path(@department), method: :patch do |f|
  .form-inputs

  	= f.input :name, label: "Название"
    = f.button :submit, "Сохранить", class: "btn-block"