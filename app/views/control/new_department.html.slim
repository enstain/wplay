= render "partials/flash"

= simple_form_for @department, url: control_create_department_path() do |f|
  .form-inputs

  	= f.input :name, label: "Название"
    = f.button :submit, "Добавить", class: "btn-block"